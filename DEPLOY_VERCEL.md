# Инструкция по деплою на Vercel (БЕСПЛАТНО)

Vercel - это самый простой и быстрый способ задеплоить ваш сайт. Полностью бесплатно!

## Способ 1: Деплой через веб-интерфейс Vercel (Рекомендуется)

### Шаг 1: Загрузите код на GitHub

Если вы еще не загрузили код:

```bash
cd /Users/dr/Downloads/ai-key
git add .
git commit -m "Prepare for Vercel deployment"
git push origin main
```

### Шаг 2: Зарегистрируйтесь на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"** (самый простой способ)
4. Авторизуйтесь через GitHub

### Шаг 3: Импортируйте проект

1. После входа нажмите **"Add New..."** → **"Project"**
2. Найдите ваш репозиторий `AIAgregator-Landing`
3. Нажмите **"Import"**

### Шаг 4: Настройте проект

Vercel автоматически определит настройки:
- **Framework Preset**: Vite (определится автоматически)
- **Root Directory**: `./` (оставьте как есть)
- **Build Command**: `npm run build` (уже указано)
- **Output Directory**: `dist` (уже указано)
- **Install Command**: `npm install` (уже указано)

**Просто нажмите "Deploy"!**

### Шаг 5: Готово! 🎉

Через 1-2 минуты ваш сайт будет доступен по адресу:
- `https://ai-agregator-landing.vercel.app` (или похожий URL)

Vercel автоматически создаст красивый домен для вашего проекта.

## Способ 2: Деплой через Vercel CLI (Альтернативный)

### Шаг 1: Установите Vercel CLI

```bash
npm install -g vercel
```

### Шаг 2: Войдите в Vercel

```bash
vercel login
```

### Шаг 3: Задеплойте проект

```bash
cd /Users/dr/Downloads/ai-key
vercel
```

Следуйте инструкциям в терминале. Vercel задаст несколько вопросов:
- Set up and deploy? **Y**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **N** (для первого деплоя)
- Project name? Нажмите Enter (используется имя папки)
- Directory? Нажмите Enter (используется текущая папка)

### Шаг 4: Для продакшн деплоя

```bash
vercel --prod
```

## Обновление сайта

### Автоматическое обновление (рекомендуется)

Если вы подключили GitHub репозиторий:
1. Просто делайте `git push` в ветку `main`
2. Vercel автоматически задеплоит новую версию!

### Ручное обновление

```bash
vercel --prod
```

## Настройка кастомного домена (опционально)

1. В панели Vercel откройте ваш проект
2. Перейдите в **Settings** → **Domains**
3. Добавьте ваш домен
4. Следуйте инструкциям по настройке DNS

## Преимущества Vercel

✅ **Полностью бесплатно** для личных проектов  
✅ **Автоматический HTTPS**  
✅ **CDN по всему миру** (быстрая загрузка)  
✅ **Автоматический деплой** при каждом push  
✅ **Preview деплои** для каждого PR  
✅ **Простая настройка** - работает из коробки  

## Возможные проблемы

### Ошибка сборки

- Проверьте, что все зависимости указаны в `package.json`
- Убедитесь, что команда `npm run build` работает локально
- Проверьте логи в панели Vercel

### Сайт не открывается

- Подождите 1-2 минуты после деплоя
- Проверьте статус деплоя в панели Vercel
- Убедитесь, что деплой завершился успешно

### Неправильные пути

- Убедитесь, что в `vite.config.ts` установлен `base: '/'`
- Файл `vercel.json` уже настроен для правильной маршрутизации

## Сравнение с GitHub Pages

| Функция | Vercel | GitHub Pages |
|---------|--------|--------------|
| Бесплатно | ✅ Да | ✅ Да (публичные репозитории) |
| Автоматический HTTPS | ✅ Да | ✅ Да |
| CDN | ✅ Да | ⚠️ Ограниченный |
| Preview деплои | ✅ Да | ❌ Нет |
| Простота настройки | ✅ Очень просто | ⚠️ Требует настройки |
| Кастомный домен | ✅ Да | ✅ Да |

**Вывод**: Vercel - лучший выбор для деплоя React/Vite приложений! 🚀

