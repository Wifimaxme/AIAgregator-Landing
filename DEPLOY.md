# Инструкция по деплою на GitHub Pages

## ⚠️ Важно: GitHub Pages БЕСПЛАТЕН для публичных репозиториев!

GitHub Pages полностью бесплатен, если ваш репозиторий **публичный (Public)**. Платная подписка нужна только для приватных репозиториев.

## Шаг 1: Проверка репозитория

1. Перейдите в ваш репозиторий: https://github.com/Wifimaxme/AIAgregator-Landing
2. Убедитесь, что репозиторий **публичный (Public)**
   - Если репозиторий приватный, перейдите в **Settings** → **General** → **Danger Zone** → **Change visibility** → **Make public**
   - Или используйте GitHub Pro (платно) для приватных репозиториев

## Шаг 2: Инициализация Git и загрузка кода

Выполните следующие команды в терминале (из директории проекта):

```bash
cd /Users/dr/Downloads/ai-key

# Инициализация git репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: AI Key landing page"

# Добавление remote репозитория (уже настроен)
# git remote add origin https://github.com/Wifimaxme/AIAgregator-Landing.git

# Переименование ветки в main (если нужно)
git branch -M main

# Загрузка кода на GitHub
git push -u origin main
```

## Шаг 3: Настройка GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** (вкладка вверху)
3. В левом меню найдите **Pages**
4. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
5. Сохраните изменения

## Шаг 4: Автоматический деплой

После настройки GitHub Actions будет автоматически деплоить ваш сайт при каждом push в ветку `main`.

**Важно**: При первом деплое может потребоваться несколько минут.

## Шаг 5: Доступ к сайту

После успешного деплоя ваш сайт будет доступен по адресу:
- `https://wifimaxme.github.io/AIAgregator-Landing/`

## Настройка кастомного домена (опционально)

Если вы хотите использовать свой домен:

1. В `vite.config.ts` измените `base: '/AIAgregator-Landing/'` на `base: '/'`
2. В настройках GitHub Pages добавьте ваш кастомный домен
3. Настройте DNS записи у вашего регистратора домена

## Обновление сайта

Для обновления сайта просто выполните:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически соберет и задеплоит новую версию.

## Проверка статуса деплоя

1. Перейдите в ваш репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Там вы увидите статус всех деплоев

## Частые вопросы

### GitHub требует платную подписку?

**НЕТ!** GitHub Pages полностью бесплатен для публичных репозиториев. Если GitHub показывает требование подписки:
1. Убедитесь, что репозиторий **публичный (Public)**, а не приватный
2. Проверьте, что вы используете **GitHub Actions** как источник (не ветку gh-pages)
3. Если репозиторий должен быть приватным, используйте альтернативу (Vercel, Netlify) или GitHub Pro

### Репозиторий приватный, что делать?

У вас есть два варианта:
1. **Сделать репозиторий публичным** (рекомендуется для лендингов)
2. **Использовать альтернативный хостинг** - см. `DEPLOY_VERCEL.md`

## Возможные проблемы

### Сайт не открывается
- Убедитесь, что деплой завершился успешно (вкладка Actions)
- Проверьте, что в `vite.config.ts` указан правильный `base` путь
- Подождите несколько минут после первого деплоя

### Неправильные пути к ресурсам
- Убедитесь, что `base` в `vite.config.ts` соответствует имени репозитория
- Для репозитория `AIAgregator-Landing` base должен быть `/AIAgregator-Landing/`

### Ошибки сборки
- Проверьте логи в разделе Actions
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что нет синтаксических ошибок в коде

